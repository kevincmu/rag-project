Below is an example of what the project looks like:

![example](example.png)

To start the application, run ```uvicorn app.main:app --reload``` in the root directory, and go to ```http://127.0.0.1:8000/docs```